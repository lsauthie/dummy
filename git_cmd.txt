git commit -m "message"
git add file
git push origin master
git pull origin master (to get update)


>> fixtures, i.e. replace the db ------------
manage.py dumpdata #copy them into a fixtures
maange.py loaddata dump #file fixtures

#heroku ---------------------
heroku help
git push heroku master (for heroku)
heroku ps:scale web:0 #turn down
heroku ps:scale web:1 #turn on

>> superuser --------------------
python manage.py createsuperuser
ludovic:lsauthie83

>> reset db on heroku -------------
1. heroku run python manage.py flush
2. (if change in DB) heroku run python manage.py makemigrations (and migrate)
3. heroku run python manage.py loaddata dump